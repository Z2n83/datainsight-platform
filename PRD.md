# DataInsight — 产品需求文档 (PRD)

> **版本**：v1.0 | **状态**：MVP 规划 | **作者**：产品团队 | **日期**：2026-08-06

---

## 目录

1. [产品概述](#1-产品概述)
2. [市场与用户分析](#2-市场与用户分析)
3. [用户角色与场景](#3-用户角色与场景)
4. [产品目标与成功指标](#4-产品目标与成功指标)
5. [信息架构](#5-信息架构)
6. [功能架构](#6-功能架构)
7. [核心业务流程](#7-核心业务流程)
8. [页面详细设计](#8-页面详细设计)
9. [非功能需求](#9-非功能需求)
10. [指标体系](#10-指标体系)
11. [版本规划](#11-版本规划)
12. [附录](#12-附录)

---

## 1. 产品概述

### 1.1 产品定位

DataInsight 是一款面向企业运营管理、设备管理和数据分析人员的 **数据分析与可视化管理平台**（SaaS）。

**一句话描述**：将企业原始业务数据经过采集、清洗、标准化、指标计算、分析、可视化、异常识别、预警、智能巡检，最终转化为能够辅助企业运营决策的数据产品。

### 1.2 核心价值主张

| 痛点 | 解决方案 |
|------|---------|
| 企业数据来源分散，无法统一查看 | 多数据源接入与统一数据管理 |
| 原始数据存在缺失、重复和异常 | 数据质量监控与自动清洗 |
| 管理人员无法快速掌握运营状态 | 运营工作台一屏总览 |
| 指标分析依赖人工 Excel 处理 | 灵活的多维度分析引擎 |
| 异常发现滞后，影响业务 | 实时监控 + 规则引擎主动预警 |
| 历史趋势难以分析 | 多时间粒度的趋势分析工具 |
| 分析结果无法形成管理动作 | 预警→处理→巡检→再分析的闭环 |

### 1.3 产品边界

**DataInsight 做什么**：
- ✅ 企业运营数据的统一管理与分析
- ✅ 设备运行状态的监控与巡检
- ✅ 异常自动识别与预警推送
- ✅ 灵活的可视化看板搭建
- ✅ 数据报表自动生成与推送

**DataInsight 不做什么**：
- ❌ 不替代企业的业务系统（ERP、CRM）
- ❌ 不提供原始数据的增删改（仅读取与分析）
- ❌ 不替代 BI 工具的高级自助分析能力

---

## 2. 市场与用户分析

### 2.1 目标市场

- **行业**：制造业、能源、物流、园区管理、连锁零售
- **企业规模**：100-5000 人的中型企业
- **部门**：运营管理部、设备管理部、数据分析团队

### 2.2 用户痛点矩阵

| 角色 | 核心痛点 | 频率 | 严重程度 |
|------|---------|------|---------|
| 运营经理 | 每天需要看多个系统才能了解运营全貌 | 每日 | 高 |
| 运营经理 | 异常发生后才知道，无法提前预警 | 每周 | 严重 |
| 数据分析师 | 80% 时间花在数据清洗，只有 20% 分析 | 每日 | 高 |
| 数据分析师 | 分析结果难以沉淀和复用 | 每周 | 中 |
| 设备管理员 | 无法远程掌握设备状态 | 每日 | 高 |
| 设备管理员 | 巡检靠纸质记录，效率低 | 每日 | 中 |
| 系统管理员 | 数据权限管理复杂 | 每月 | 中 |

---

## 3. 用户角色与场景

### 3.1 角色定义

#### 系统管理员 (System Admin)
- **职责**：用户管理、角色权限配置、数据源配置、系统参数设置
- **使用频率**：每周 2-3 次
- **核心关注**：系统稳定性、数据安全、权限合规

#### 运营经理 (Operations Manager)
- **职责**：查看运营总览、监控核心指标、处理预警、查看报表
- **使用频率**：每日多次
- **核心关注**：核心 KPI 变化、异常事件、趋势走向

#### 数据分析师 (Data Analyst)
- **职责**：数据集管理、指标分析、趋势分析、异常分析、自定义看板
- **使用频率**：每日 4-6 小时
- **核心关注**：数据质量、分析效率、洞察深度

#### 设备管理员 (Device Manager)
- **职责**：设备状态监控、故障处理、巡检任务执行、巡检记录
- **使用频率**：每日多次
- **核心关注**：设备运行状态、故障及时性、巡检完成率

#### 普通员工 (Regular Staff)
- **职责**：查看被授权的数据和看板
- **使用频率**：每周 1-2 次
- **核心关注**：与自己相关的数据指标

### 3.2 核心用户场景

#### 场景 1：运营经理每日晨检
```
08:30  登录系统，进入运营总览看板
08:31  查看核心 KPI（数据总量、正常运行率、异常数量、设备利用率）
08:32  查看趋势图（最近 7 天数据量趋势、设备运行趋势）
08:33  查看待办（待处理预警 3 条，待巡检设备 5 台，数据源异常 1 个）
08:35  点击查看高危预警详情
08:37  指派预警给相关负责人处理
08:40  查看昨日运营数据报表
08:45  结束晨检，开始处理具体事项
```

#### 场景 2：数据分析师日常分析
```
09:00  登录系统，进入数据分析模块
09:02  选择数据集"设备运行数据"
09:03  选择指标"设备利用率"、"故障次数"
09:04  选择维度"按设备类型"、"按时间（日）"
09:05  设置时间范围"最近 30 天"
09:06  添加筛选条件"设备状态 = 运行中"
09:07  选择分析方式"趋势分析"
09:08  查看生成的趋势图和指标摘要
09:10  发现设备利用率在周末明显下降
09:12  切换到对比分析，对比工作日 vs 周末
09:15  导出分析结果，准备汇报材料
09:20  将分析结果添加到自定义看板
```

#### 场景 3：设备管理员处理预警
```
10:00  收到预警通知（短信 + 系统内通知）
10:02  登录系统，进入预警中心
10:03  查看预警详情：设备 A-203 温度超过阈值（85°C > 80°C），持续 15 分钟
10:05  确认预警，标记为"处理中"
10:06  联系现场人员核实设备状态
10:20  现场反馈：设备散热风扇故障
10:22  创建巡检任务，安排维修
10:25  在系统中记录处理结果
10:26  关闭预警
```

#### 场景 4：系统管理员季度权限审计
```
14:00  登录系统，进入系统管理
14:02  查看用户列表，确认在职/离职状态
14:05  调整离职人员账号状态为"已禁用"
14:08  查看角色权限配置
14:10  根据审计要求调整 2 个角色的数据权限
14:15  导出操作日志，归档备查
14:20  完成审计
```

---

## 4. 产品目标与成功指标

### 4.1 产品目标

| 目标 | 描述 | 衡量方式 |
|------|------|---------|
| **提升运营效率** | 运营经理获取全局状态从 30 分钟缩短到 3 分钟 | 使用前后时间对比 |
| **降低异常损失** | 异常发现时间从小时级缩短到分钟级 | 预警触发到响应的时间差 |
| **提高分析效率** | 数据分析师从数据准备中释放 60% 时间 | 用户反馈问卷 |
| **规范化巡检** | 巡检从纸质记录转为系统化管理 | 巡检任务完成率 |
| **数据资产沉淀** | 形成可复用的数据集、指标和分析模板 | 系统内可复用资产数量 |

### 4.2 产品指标体系

| 类别 | 指标 | 目标值 | 测量方式 |
|------|------|--------|---------|
| **数据质量** | 数据采集成功率 | ≥ 99.5% | 采集日志 |
| | 数据完整率 | ≥ 98% | 数据质量检测 |
| | 数据准确率 | ≥ 99% | 抽样校验 |
| **性能** | Dashboard 首屏加载 | < 2s | Lighthouse |
| | 千级数据分析耗时 | < 5s | API 计时 |
| | 全量分析耗时 | < 30s | API 计时 |
| | API 响应 P95 | < 500ms | APM |
| **业务** | 异常发现率 | ≥ 95% | 预警统计 |
| | 预警触达率 | ≥ 99% | 通知记录 |
| | 预警处理率 | ≥ 90% | 预警状态统计 |
| | 巡检完成率 | ≥ 95% | 巡检记录 |
| **用户** | 日活用户数 | 增长趋势 | 埋点 |
| | 用户任务完成率 | ≥ 85% | 用户测试 |
| | 用户满意度 | ≥ 4.0/5.0 | 满意度问卷 |

---

## 5. 信息架构

### 5.1 导航结构

```
工作台
├── 运营总览
├── 我的任务
└── 最近访问

数据管理
├── 数据源           → 列表 + 详情
│   └── 数据源详情
├── 数据集           → 列表 + 详情
│   └── 数据集详情
├── 数据质量         → 质量报告
└── 数据更新记录     → 同步日志

数据分析
├── 指标分析         → 分析工作台
├── 趋势分析         → 趋势图 + 洞察
├── 对比分析         → 对比配置 + 结果
└── 异常分析         → 异常列表 + 详情

可视化看板
├── 我的看板         → 个人收藏
├── 企业总览         → 默认企业看板
├── 设备运营看板     → 设备专用看板
└── 自定义看板       → 看板编辑器
    └── 看板详情

预警中心
├── 实时预警         → 当前活跃预警
├── 预警规则         → 规则列表 + 编辑
│   └── 规则详情
├── 预警记录         → 历史预警查询
└── 已处理           → 已关闭预警归档

智能巡检
├── 巡检任务         → 任务列表 + 执行
│   └── 任务详情
├── 巡检计划         → 计划配置
├── 异常设备         → 异常设备列表
└── 巡检记录         → 历史记录查询

数据报表
├── 报表中心         → 报表列表
├── 定时报表         → 定时配置
└── 导出记录         → 导出历史

系统管理
├── 用户管理         → 用户列表 + 编辑
├── 角色权限         → 角色列表 + 权限树
├── 操作日志         → 日志查询
└── 系统配置         → 参数配置
```

### 5.2 页面清单

| 编号 | 路由 | 页面名称 | 所属模块 | 优先级 |
|------|------|---------|---------|--------|
| P01 | `/login` | 登录页 | 公共 | P0 |
| P02 | `/dashboard` | 运营总览 | 工作台 | P0 |
| P03 | `/data-sources` | 数据源列表 | 数据管理 | P0 |
| P04 | `/data-sources/:id` | 数据源详情 | 数据管理 | P0 |
| P05 | `/datasets` | 数据集列表 | 数据管理 | P0 |
| P06 | `/datasets/:id` | 数据集详情 | 数据管理 | P0 |
| P07 | `/data-quality` | 数据质量 | 数据管理 | P1 |
| P08 | `/data-sync-logs` | 数据更新记录 | 数据管理 | P1 |
| P09 | `/data-analysis` | 指标分析工作台 | 数据分析 | P0 |
| P10 | `/trend-analysis` | 趋势分析 | 数据分析 | P1 |
| P11 | `/compare-analysis` | 对比分析 | 数据分析 | P1 |
| P12 | `/anomaly-analysis` | 异常分析 | 数据分析 | P1 |
| P13 | `/dashboards` | 看板列表 | 可视化看板 | P0 |
| P14 | `/dashboards/:id` | 看板详情 | 可视化看板 | P0 |
| P15 | `/alerts` | 实时预警 | 预警中心 | P1 |
| P16 | `/alert-rules` | 预警规则 | 预警中心 | P1 |
| P17 | `/alert-records` | 预警记录 | 预警中心 | P1 |
| P18 | `/alerts/processed` | 已处理预警 | 预警中心 | P1 |
| P19 | `/inspections` | 巡检任务 | 智能巡检 | P2 |
| P20 | `/inspection-plans` | 巡检计划 | 智能巡检 | P2 |
| P21 | `/inspection-records` | 巡检记录 | 智能巡检 | P2 |
| P22 | `/abnormal-devices` | 异常设备 | 智能巡检 | P2 |
| P23 | `/reports` | 报表中心 | 数据报表 | P2 |
| P24 | `/report-schedules` | 定时报表 | 数据报表 | P2 |
| P25 | `/export-records` | 导出记录 | 数据报表 | P2 |
| P26 | `/users` | 用户管理 | 系统管理 | P2 |
| P27 | `/roles` | 角色权限 | 系统管理 | P2 |
| P28 | `/logs` | 操作日志 | 系统管理 | P2 |
| P29 | `/settings` | 系统配置 | 系统管理 | P2 |
| P30 | `/versions` | 版本信息 | 系统管理 | P2 |

---

## 6. 功能架构

### 6.1 功能模块总览

```
DataInsight 功能架构
│
├── 1.0 工作台模块
│   ├── 1.1 运营总览（KPI + 趋势 + 状态 + 待办）
│   ├── 1.2 我的任务
│   └── 1.3 最近访问
│
├── 2.0 数据管理模块
│   ├── 2.1 数据源管理（CRUD + 连接测试 + 同步配置）
│   ├── 2.2 数据集管理（CRUD + 字段映射 + 预览）
│   ├── 2.3 数据质量管理（质量检测 + 报告 + 修复建议）
│   └── 2.4 数据同步（全量/增量 + 定时 + 重试 + 日志）
│
├── 3.0 数据分析模块
│   ├── 3.1 指标分析引擎（数据集→指标→维度→时间→筛选→分析方式）
│   ├── 3.2 趋势分析（多粒度时间序列）
│   ├── 3.3 对比分析（同比/环比/自定义对比）
│   ├── 3.4 排名分析（Top N / Bottom N）
│   └── 3.5 异常分析（统计异常 + 规则异常）
│
├── 4.0 可视化看板模块
│   ├── 4.1 预制看板（企业总览 / 设备运营）
│   ├── 4.2 自定义看板编辑器（拖拽布局 + 组件配置）
│   ├── 4.3 图表组件库（20+ 图表类型）
│   └── 4.4 看板收藏与分享
│
├── 5.0 预警中心模块
│   ├── 5.1 预警规则引擎
│   ├── 5.2 实时预警列表
│   ├── 5.3 预警处理流程（确认→处理→关闭）
│   ├── 5.4 多渠道通知
│   └── 5.5 预警归档与统计
│
├── 6.0 智能巡检模块
│   ├── 6.1 巡检计划管理
│   ├── 6.2 巡检任务执行
│   ├── 6.3 异常设备追踪
│   └── 6.4 巡检记录归档
│
├── 7.0 数据报表模块
│   ├── 7.1 报表模板
│   ├── 7.2 报表生成（即时 + 定时）
│   ├── 7.3 多格式导出
│   └── 7.4 报表归档
│
└── 8.0 系统管理模块
    ├── 8.1 用户管理
    ├── 8.2 角色与权限管理 (RBAC)
    ├── 8.3 操作日志
    └── 8.4 系统配置
```

---

## 7. 核心业务流程

### 7.1 数据全链路流程

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  数据源   │───→│  数据采集  │───→│  数据清洗  │───→│ 数据标准化 │
│ (DB/API/ │    │ (连接/抽取)│    │(去重/补全)│    │(格式/单位)│
│  File)   │    │           │    │           │    │           │
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                                                       │
                                                       ▼
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  巡检     │←───│  处理记录  │←───│  人工处理  │←───│  预警     │
│ (验证/复查)│   │ (归档沉淀) │    │(派发/执行)│    │(触发/通知)│
└──────────┘    └──────────┘    └──────────┘    └──────────┘
      │                                               ↑
      └─────────────────── 反馈 ──────────────────────┘

中间环节（不展开连线）：
  数据集 → 指标计算 → 数据分析 → 可视化 → 异常检测
```

### 7.2 各节点详细说明

| 节点 | 输入 | 处理过程 | 输出 | 负责人 |
|------|------|---------|------|--------|
| **数据采集** | 数据源连接信息 | 连接数据源、抽取数据、记录采集日志 | 原始数据 + 采集日志 | 系统管理员 |
| **数据清洗** | 原始数据 | 去重、缺失值处理、异常值标记、格式校验 | 清洗后数据 + 质量报告 | 系统/分析师 |
| **数据标准化** | 清洗后数据 | 字段映射、单位统一、编码转换、类型标准化 | 标准化数据 | 系统自动 |
| **数据集** | 标准化数据 | 表关联、字段选择、数据聚合、虚拟字段计算 | 可分析的数据集 | 数据分析师 |
| **指标计算** | 数据集 | 聚合计算（SUM/AVG/COUNT/MAX/MIN）、衍生指标 | 指标值 | 系统自动 |
| **数据分析** | 指标值 | 趋势/对比/排名/异常分析 | 分析结果 + 图表 | 数据分析师 |
| **可视化** | 分析结果 | 图表渲染、看板布局、数据映射 | 可视化看板 | 运营经理/分析师 |
| **异常检测** | 指标值 + 规则 | 阈值比较、趋势偏离、模式识别 | 异常事件 | 系统自动 |
| **预警** | 异常事件 + 规则 | 等级判定、通知发送、创建待办 | 预警通知 | 系统自动 |
| **人工处理** | 预警信息 | 确认、分析、指派、执行修复 | 处理记录 | 运营经理/设备管理员 |
| **巡检** | 处理记录 + 计划 | 验证修复、复查状态、记录结果 | 巡检记录 | 设备管理员 |
| **再分析** | 处理记录 + 巡检记录 | 效果评估、趋势回归、规则优化 | 优化建议 | 数据分析师 |

### 7.3 预警处理闭环流程

```
指标异常发生
      │
      ▼
┌─────────────┐
│ 规则引擎匹配  │ ← 指标 + 条件 + 阈值 + 持续时间
└─────────────┘
      │
      ▼
┌─────────────┐
│ 生成预警     │ ← 记录：时间、指标、值、阈值、等级
└─────────────┘
      │
      ▼
┌─────────────┐     ┌──────────┐
│ 等级判定     │────→│ 严重/高  │────→ 立即通知（短信+电话）
│             │     ├──────────┤
│             │     │ 中       │────→ 通知（邮件+系统内）
│             │     ├──────────┤
│             │     │ 低       │────→ 系统内通知
└─────────────┘     └──────────┘
      │
      ▼
┌─────────────┐
│ 通知负责人   │ ← 根据规则配置的通知方式和负责人
└─────────────┘
      │
      ▼
┌─────────────┐
│ 负责人确认   │ ← 状态：待处理 → 处理中
└─────────────┘
      │
      ▼
┌─────────────┐
│ 执行处理     │ ← 记录处理措施
└─────────────┘
      │
      ▼
┌─────────────┐
│ 验证结果     │ ← 可选：创建巡检任务验证
└─────────────┘
      │
      ▼
┌─────────────┐
│ 关闭预警     │ ← 状态：已关闭，记录处理全流程
└─────────────┘
      │
      ▼
┌─────────────┐
│ 归档沉淀     │ ← 可用于后续分析和规则优化
└─────────────┘
```

### 7.4 数据分析工作流

```
用户进入分析工作台
      │
      ▼
┌─────────────────┐
│ Step 1: 选择数据集 │ → 从数据集列表中选择一个
└─────────────────┘
      │
      ▼
┌─────────────────┐
│ Step 2: 选择指标  │ → 从数据集中选择要分析的指标（多选）
└─────────────────┘
      │
      ▼
┌─────────────────┐
│ Step 3: 选择维度  │ → 选择分组维度（时间/设备类型/区域等）
└─────────────────┘
      │
      ▼
┌─────────────────┐
│ Step 4: 时间范围  │ → 选择分析时间窗口
└─────────────────┘
      │
      ▼
┌─────────────────┐
│ Step 5: 筛选条件  │ → 添加可选的筛选条件（可选）
└─────────────────┘
      │
      ▼
┌─────────────────┐
│ Step 6: 分析方式  │ → 趋势/对比/排名/异常
└─────────────────┘
      │
      ▼
┌─────────────────┐
│ 生成分析结果      │
│ ├── 图表          │
│ ├── 数据表        │
│ ├── 指标摘要      │
│ └── 数据洞察      │
└─────────────────┘
      │
      ▼
┌─────────────────┐
│ 后续操作          │
│ ├── 保存分析配置   │
│ ├── 添加到看板    │
│ ├── 导出数据      │
│ └── 创建预警规则   │
└─────────────────┘
```

---

## 8. 页面详细设计

### 8.1 登录页 `/login`

| 属性 | 描述 |
|------|------|
| **页面目标** | 用户身份认证，进入系统 |
| **目标用户** | 所有角色 |
| **核心任务** | 输入账号密码登录 |
| **页面布局** | 居中卡片式登录表单 |
| **核心组件** | 用户名输入框、密码输入框、登录按钮、记住我、忘记密码链接 |
| **数据字段** | username, password, rememberMe |
| **用户操作** | 登录、重置密码 |
| **页面状态** | 正常、加载中（登录请求）、错误（账号或密码错误）、锁定（多次失败） |
| **权限要求** | 无（公开页面） |

### 8.2 运营总览（Dashboard）`/dashboard`

| 属性 | 描述 |
|------|------|
| **页面目标** | 登录后首页，一屏掌握运营全局 |
| **目标用户** | 运营经理（主）、数据分析师、系统管理员 |
| **核心任务** | 查看核心KPI、趋势、状态分布、处理待办 |
| **页面布局** | 顶部 KPI 卡片行 → 中间趋势图区（2列） → 底部状态分布 + 待办列表（2列） |
| **核心组件** | StatCard（KPI卡片）、LineChart（趋势图）、PieChart（状态分布）、TodoList（待办列表） |
| **数据字段** | totalDataVolume, todayNewData, normalOperationRate, anomalyCount, deviceUtilization, dataVolumeTrend[], deviceStatusDistribution, pendingAlerts[], pendingInspections[] |
| **用户操作** | 点击KPI下钻、点击待办跳转处理、切换时间范围 |
| **页面状态** | 正常、加载中、数据为空（首次使用）、部分加载失败 |
| **权限要求** | 登录即可访问，数据范围受角色限制 |
| **业务逻辑** | KPI之间必须存在业务关系：正常运行率 = 正常设备数/总设备数；异常数量 = 故障+数据异常+通信异常的总和；设备利用率 = 实际运行时长/计划运行时长 |

### 8.3 数据源列表 `/data-sources`

| 属性 | 描述 |
|------|------|
| **页面目标** | 管理所有数据源连接 |
| **目标用户** | 系统管理员、数据分析师 |
| **核心任务** | 查看数据源状态、新增数据源、测试连接、配置同步 |
| **页面布局** | 顶部操作栏（搜索+筛选+新增按钮）→ 数据源卡片/表格列表 → 分页 |
| **核心组件** | DataSourceTable、StatusBadge、SearchBar、Pagination |
| **数据字段** | id, name, type(MySQL/PostgreSQL/MongoDB/API/CSV), status(connected/disconnected/error), dataVolume, syncMethod(full/incremental), syncFrequency, lastSyncTime, owner |
| **用户操作** | 搜索、筛选（按类型/状态）、新增数据源、编辑、删除、测试连接、手动同步 |
| **页面状态** | 正常、加载中、空列表、搜索无结果、操作确认弹窗 |
| **权限要求** | 管理员：全部操作；分析师：查看+新增；其他角色：仅查看 |

### 8.4 数据源详情 `/data-sources/:id`

| 属性 | 描述 |
|------|------|
| **页面目标** | 查看和编辑单个数据源的完整信息 |
| **目标用户** | 系统管理员、数据分析师 |
| **核心任务** | 查看连接信息、数据预览、同步日志、编辑配置 |
| **页面布局** | Tab切换：基本信息 | 数据预览 | 同步配置 | 同步日志 |
| **核心组件** | InfoCard, DataPreviewTable, SyncConfigForm, SyncLogTimeline |
| **数据字段** | 同列表 + connectionDetails, tables[], syncHistory[], dataQualityScore |
| **用户操作** | 编辑、测试连接、手动同步、删除 |
| **页面状态** | 正常、加载中、不存在(404)、编辑模式 |
| **权限要求** | 管理员：全部；分析师：查看+同步；其他：仅查看 |

### 8.5 数据集列表 `/datasets`

| 属性 | 描述 |
|------|------|
| **页面目标** | 管理所有数据集 |
| **目标用户** | 数据分析师、系统管理员 |
| **核心任务** | 查看数据集、新建数据集、预览数据 |
| **页面布局** | 顶部操作栏 → 数据集表格 → 分页 |
| **核心组件** | DatasetTable, CreateDatasetModal, SearchBar, Pagination |
| **数据字段** | id, name, sourceName, fieldCount, dataVolume, updatedAt, owner |
| **用户操作** | 搜索、新建、编辑、删除、预览数据 |
| **页面状态** | 正常、加载中、空列表、新建/编辑弹窗 |
| **权限要求** | 管理员：全部；分析师：CRUD；运营经理：查看 |

### 8.6 数据集详情 `/datasets/:id`

| 属性 | 描述 |
|------|------|
| **页面目标** | 查看和编辑数据集详情 |
| **目标用户** | 数据分析师、系统管理员 |
| **核心任务** | 查看字段信息、数据预览、修改配置、查看关联看板 |
| **页面布局** | Tab切换：基本信息 | 字段列表 | 数据预览 | 数据质量 | 关联看板 |
| **核心组件** | FieldTable, DataPreviewGrid, QualityReportCard, RelatedDashboards |
| **数据字段** | id, name, description, sourceId, fields[]{name,type,alias}, rowCount, qualityScore, dashboards[] |
| **用户操作** | 编辑、预览、刷新数据、删除 |
| **页面状态** | 正常、加载中、不存在(404)、编辑模式 |
| **权限要求** | 管理员：全部；分析师：CRUD；其他：查看 |

### 8.7 指标分析工作台 `/data-analysis`

| 属性 | 描述 |
|------|------|
| **页面目标** | 核心分析工作台，灵活配置分析参数 |
| **目标用户** | 数据分析师、运营经理 |
| **核心任务** | 配置分析参数 → 生成分析结果 → 获取数据洞察 |
| **页面布局** | 左侧配置面板（数据集→指标→维度→时间→筛选→分析方式）→ 右侧结果展示（图表+表格+洞察） |
| **核心组件** | DatasetSelector, MetricSelector, DimensionSelector, TimeRangePicker, FilterBuilder, AnalysisTypeSelector, ChartArea, DataTable, InsightCard |
| **数据字段** | datasetId, metrics[], dimensions[], timeRange, filters[], analysisType, chartConfig |
| **用户操作** | 选择配置 → 点击分析 → 查看结果 → 切换图表类型 → 导出 → 保存配置 → 添加到看板 |
| **页面状态** | 配置中、分析中（加载）、结果展示、无数据、错误 |
| **权限要求** | 分析师：全部；运营经理：使用已有配置；其他：仅查看 |

### 8.8 趋势分析 `/trend-analysis`

| 属性 | 描述 |
|------|------|
| **页面目标** | 深入分析数据随时间变化的趋势 |
| **目标用户** | 数据分析师、运营经理 |
| **核心任务** | 查看多粒度趋势、识别趋势变化点、对比不同周期 |
| **页面布局** | 顶部配置栏 → 趋势图（全宽） → 底部趋势洞察卡片 + 数据表 |
| **核心组件** | GranularitySelector(时/日/周/月/季/年), TrendLineChart, TrendChangeMarker, SeasonalityChart, ForecastLine |
| **数据字段** | metric, granularity, timeRange, baseline, forecast |
| **用户操作** | 切换粒度、缩放时间轴、查看数据点详情、导出 |
| **页面状态** | 正常、加载中、无数据 |
| **权限要求** | 分析师：全部；运营经理：查看；其他：查看被授权数据 |

### 8.9 异常分析 `/anomaly-analysis`

| 属性 | 描述 |
|------|------|
| **页面目标** | 自动检测和展示数据异常 |
| **目标用户** | 数据分析师、运营经理、设备管理员 |
| **核心任务** | 查看异常列表、分析异常原因、关联预警 |
| **页面布局** | 顶部筛选栏 → 异常统计卡片（按类型/等级） → 异常列表 → 异常详情抽屉 |
| **核心组件** | AnomalyTypeFilter, AnomalyStatCards, AnomalyTable, AnomalyDetailDrawer, TrendOverlayChart |
| **数据字段** | anomalyType(设备故障/数据异常/数据缺失/运行异常/通信异常), severity, detectedTime, relatedMetric, currentValue, expectedValue, deviation |
| **用户操作** | 筛选、排序、查看详情、创建预警规则、标记已处理 |
| **页面状态** | 正常、加载中、无异常、详情展开 |
| **权限要求** | 分析师：全部；运营经理：查看+处理；设备管理员：设备类异常 |

### 8.10 看板列表 `/dashboards`

| 属性 | 描述 |
|------|------|
| **页面目标** | 浏览和管理所有看板 |
| **目标用户** | 所有角色 |
| **核心任务** | 查看看板列表、打开看板、创建自定义看板 |
| **页面布局** | 顶部操作栏 → 看板卡片网格（含缩略图） → 分页 |
| **核心组件** | DashboardCard（缩略图+名称+描述+更新时间）, CreateDashboardModal, CategoryTabs |
| **数据字段** | id, name, description, thumbnail, category, updatedAt, creator |
| **用户操作** | 搜索、按分类筛选、新建、编辑、删除、收藏 |
| **页面状态** | 正常、加载中、空列表 |
| **权限要求** | 管理员：全部；其他：查看被授权的看板 |

### 8.11 看板详情 `/dashboards/:id`

| 属性 | 描述 |
|------|------|
| **页面目标** | 查看和交互可视化看板 |
| **目标用户** | 所有角色 |
| **核心任务** | 查看图表、交互钻取、刷新数据 |
| **页面布局** | 顶部标题栏（名称+时间+操作按钮）→ 看板网格布局（可拖拽） |
| **核心组件** | WidgetGrid（响应式网格）, ChartWidget（含下钻/缩放）, FilterBar, AutoRefreshBadge |
| **数据字段** | dashboardId, widgets[]{id, type, config, position, data} |
| **用户操作** | 查看图表、下钻、缩放、切换时间、刷新、导出、分享 |
| **页面状态** | 正常、加载中、部分加载失败、编辑模式 |
| **权限要求** | 看板创建者：全部操作；被授权者：查看+交互 |

### 8.12 实时预警 `/alerts`

| 属性 | 描述 |
|------|------|
| **页面目标** | 查看和处理当前活跃预警 |
| **目标用户** | 运营经理、设备管理员 |
| **核心任务** | 查看预警列表、处理预警、关闭预警 |
| **页面布局** | 顶部统计卡片（按等级）→ 预警列表（实时更新）→ 预警详情抽屉 |
| **核心组件** | AlertLevelBadge, AlertTable（实时刷新）, AlertDetailDrawer, ActionButtons（确认/处理/关闭） |
| **数据字段** | id, level(low/medium/high/critical), title, metric, currentValue, threshold, triggerTime, duration, status(pending/processing/closed), assignee |
| **用户操作** | 筛选等级、确认预警、标记处理、关闭、指派、查看详情 |
| **页面状态** | 正常、加载中、无活跃预警、实时更新中 |
| **权限要求** | 运营经理：查看+处理全部；设备管理员：设备类预警；其他：仅查看 |

### 8.13 预警规则 `/alert-rules`

| 属性 | 描述 |
|------|------|
| **页面目标** | 管理预警规则配置 |
| **目标用户** | 数据分析师、运营经理 |
| **核心任务** | 创建规则、编辑规则、启用/禁用规则、查看触发历史 |
| **页面布局** | 顶部操作栏 → 规则列表（表格）→ 规则表单弹窗 |
| **核心组件** | RuleTable, RuleForm（指标+条件+阈值+持续时间+等级+负责人+通知方式）, EnableSwitch, TriggerCountBadge |
| **数据字段** | id, name, metric, condition(>/</=/!=), threshold, duration, level, assignee, notifyMethods[](system/email/sms/dingtalk), enabled, triggerCount |
| **用户操作** | 新建、编辑、删除、启用/禁用、测试规则 |
| **页面状态** | 正常、加载中、空列表、表单验证 |
| **权限要求** | 管理员：全部；分析师：CRUD；运营经理：查看+新建 |

### 8.14 巡检任务 `/inspections`

| 属性 | 描述 |
|------|------|
| **页面目标** | 管理巡检任务 |
| **目标用户** | 设备管理员、运营经理 |
| **核心任务** | 查看任务列表、执行巡检、记录结果 |
| **页面布局** | 顶部筛选栏 → 任务列表（表格）→ 执行表单弹窗 |
| **核心组件** | InspectionTable, InspectionForm, ResultRadioGroup(正常/异常/需要关注), TaskProgressBar |
| **数据字段** | id, name, scope, deviceIds[], metrics[], assignee, frequency, startTime, endTime, status, result |
| **用户操作** | 筛选、执行巡检、查看详情、编辑计划 |
| **页面状态** | 正常、加载中、空列表、执行中 |
| **权限要求** | 设备管理员：全部；运营经理：查看；其他：仅查看 |

### 8.15 报表中心 `/reports`

| 属性 | 描述 |
|------|------|
| **页面目标** | 查看和生成数据报表 |
| **目标用户** | 运营经理、数据分析师 |
| **核心任务** | 查看报表列表、生成报表、下载报表 |
| **页面布局** | 顶部操作栏 → 报表列表（表格）→ 生成报表弹窗 |
| **核心组件** | ReportTable, GenerateReportModal, ReportPreview, ExportFormatSelector(PDF/Excel/CSV) |
| **数据字段** | id, name, type(日报/周报/月报/自定义), generatedAt, status, format, downloadUrl |
| **用户操作** | 查看、生成、下载、定时配置、删除 |
| **页面状态** | 正常、加载中、生成中、下载中 |
| **权限要求** | 运营经理：全部；分析师：查看+生成；其他：仅查看 |

### 8.16 用户管理 `/users`

| 属性 | 描述 |
|------|------|
| **页面目标** | 管理系统用户 |
| **目标用户** | 系统管理员 |
| **核心任务** | 查看用户列表、新增用户、编辑用户、禁用/启用 |
| **页面布局** | 顶部操作栏 → 用户表格 → 用户表单弹窗 |
| **核心组件** | UserTable, UserForm（含角色分配）, StatusSwitch, ResetPasswordButton |
| **数据字段** | id, username, realName, email, phone, roleIds[], status(active/disabled), createdAt, lastLoginAt |
| **用户操作** | 搜索、新增、编辑、禁用/启用、重置密码、分配角色 |
| **页面状态** | 正常、加载中、表单验证、操作确认 |
| **权限要求** | 仅系统管理员 |

### 8.17 角色权限 `/roles`

| 属性 | 描述 |
|------|------|
| **页面目标** | 管理角色和权限配置 |
| **目标用户** | 系统管理员 |
| **核心任务** | 查看角色列表、编辑权限、新增角色 |
| **页面布局** | 左侧角色列表 → 右侧权限树（勾选配置） |
| **核心组件** | RoleList, PermissionTree（模块→页面→操作三级）, RoleForm |
| **数据字段** | id, name, description, permissions[]{module, page, action}, userCount |
| **用户操作** | 选择角色、勾选权限、保存、新增角色、删除角色 |
| **页面状态** | 正常、加载中、未保存提示 |
| **权限要求** | 仅系统管理员 |

### 8.18 操作日志 `/logs`

| 属性 | 描述 |
|------|------|
| **页面目标** | 查询和审计用户操作记录 |
| **目标用户** | 系统管理员 |
| **核心任务** | 搜索日志、筛选、导出 |
| **页面布局** | 顶部筛选栏（用户/操作/时间/模块）→ 日志表格 |
| **核心组件** | LogFilterBar, LogTable（时间/用户/操作/模块/详情/IP）, ExportButton |
| **数据字段** | id, userId, username, action, module, targetType, targetId, detail, ip, userAgent, createdAt |
| **用户操作** | 搜索、筛选、查看详情、导出 |
| **页面状态** | 正常、加载中、搜索无结果 |
| **权限要求** | 仅系统管理员 |

### 8.19 版本信息 `/versions`

| 属性 | 描述 |
|------|------|
| **页面目标** | 展示系统版本和更新日志 |
| **目标用户** | 所有角色 |
| **核心任务** | 查看当前版本、浏览更新日志 |
| **页面布局** | 当前版本卡片 → 版本时间线 |
| **核心组件** | VersionCard, ChangelogTimeline |
| **数据字段** | version, releaseDate, changes[]{type, description}, status |
| **用户操作** | 浏览 |
| **页面状态** | 正常 |
| **权限要求** | 登录即可访问 |

---

## 9. 非功能需求

### 9.1 性能需求

| 需求 | 指标 | 优先级 |
|------|------|--------|
| 页面首屏加载 | < 2s（P95） | P0 |
| API 响应时间 | < 500ms（P95） | P0 |
| 千条数据分析 | < 5s | P0 |
| 全量数据分析 | < 30s（10万条内） | P1 |
| WebSocket 推送延迟 | < 1s | P1 |
| 并发用户数 | ≥ 500 | P1 |
| 数据采集吞吐量 | ≥ 10000条/分钟 | P2 |

### 9.2 安全需求

| 需求 | 说明 |
|------|------|
| 身份认证 | JWT Token + 刷新机制，Token 有效期 2h |
| 密码安全 | bcrypt 加密存储，复杂度要求（8位+大小写+数字+特殊字符） |
| 权限控制 | RBAC 模型，API 级别权限校验 |
| 数据隔离 | 不同角色只能看到被授权的数据源和看板 |
| SQL 注入防护 | ORM 参数化查询 |
| XSS 防护 | 输入输出转义，CSP 头配置 |
| CSRF 防护 | Token 验证 |
| 操作审计 | 全量操作日志，不可删除 |
| 传输安全 | HTTPS 加密传输 |

### 9.3 可用性需求

| 需求 | 说明 |
|------|------|
| 系统可用性 | ≥ 99.9%（全年宕机 < 8.76h） |
| 数据备份 | 每日全量备份 + 每小时增量备份 |
| 灾难恢复 | RPO < 1h，RTO < 4h |
| 优雅降级 | 非核心服务故障时核心功能仍然可用 |

### 9.4 兼容性需求

| 需求 | 说明 |
|------|------|
| 浏览器 | Chrome 90+, Edge 90+, Firefox 90+, Safari 15+ |
| 分辨率 | 1366×768 及以上（响应式适配） |
| 移动端 | v2.0 规划移动端适配（MVP 仅桌面端） |

---

## 10. 指标体系

详见 [PRODUCT_ARCHITECTURE.md](./PRODUCT_ARCHITECTURE.md) 中的指标体系章节。

---

## 11. 版本规划

详见 [RELEASE_PLAN.md](./RELEASE_PLAN.md)。

### 版本优先级逻辑

**v1.0 MVP** 优先上线核心数据链路（数据源→数据集→Dashboard），验证产品价值：
- 为什么先做数据源和数据集？→ 没有数据就没有分析对象
- 为什么先做基础 Dashboard？→ 这是用户每日高频使用的入口
- 为什么先做指标分析而不是趋势分析？→ 指标是分析的最小单元

**v1.1** 增强分析深度：
- 趋势分析、异常分析 → 从"看数据"到"分析数据"
- 预警中心 → 从"被动查看"到"主动发现"

**v1.2** 完善运营闭环：
- 智能巡检 → 补全"处理→验证"环节
- 报表中心 → 满足正式汇报需求
- 权限管理 → 企业级多角色的必要条件

**v2.0** 智能化升级：
- 高级分析 → 从描述性分析到预测性分析
- 智能洞察 → AI 辅助的数据发现

---

## 12. 附录

### 12.1 术语表

| 术语 | 英文 | 说明 |
|------|------|------|
| 数据源 | Data Source | 原始数据的来源，如数据库、API、文件 |
| 数据集 | Dataset | 经过清洗和标准化后可供分析的数据集合 |
| 指标 | Metric | 可量化的业务衡量标准，如设备利用率 |
| 维度 | Dimension | 指标的分析角度，如按时间、按设备类型 |
| 看板 | Dashboard | 可视化图表组成的监控面板 |
| 预警 | Alert | 基于规则触发的异常通知 |
| 巡检 | Inspection | 对设备和数据状态的定期检查 |

### 12.2 参考文档

- [PRODUCT_ARCHITECTURE.md](./PRODUCT_ARCHITECTURE.md) — 产品架构详细设计
- [DATABASE_DESIGN.md](./DATABASE_DESIGN.md) — 数据库设计
- [API.md](./API.md) — API 接口规划
- [RELEASE_PLAN.md](./RELEASE_PLAN.md) — 版本发布计划

---

> **文档维护**：本 PRD 由产品团队维护，随产品迭代更新。最新版本以 Git 仓库为准。
