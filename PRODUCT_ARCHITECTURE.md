# DataInsight — 产品架构文档

> **版本**：v1.0 | **状态**：MVP 规划 | **日期**：2026-08-06

---

## 目录

1. [产品定位](#1-产品定位)
2. [产品全景图](#2-产品全景图)
3. [业务架构](#3-业务架构)
4. [应用架构](#4-应用架构)
5. [数据架构](#5-数据架构)
6. [技术架构](#6-技术架构)
7. [部署架构](#7-部署架构)
8. [安全架构](#8-安全架构)
9. [指标体系](#9-指标体系)
10. [未来扩展能力](#10-未来扩展能力)

---

## 1. 产品定位

### 1.1 一句话定位

**DataInsight 是面向企业运营与管理人员的数据分析与可视化管理平台**，通过统一数据接入、数据治理、指标分析、异常预警和智能巡检，帮助企业从分散数据中快速发现异常并形成管理闭环。

### 1.2 产品边界

**DataInsight 做什么**：
- 统一接入企业分散的业务数据（MySQL、CSV）
- 数据清洗、标准化，形成可分析的数据集
- 多维度指标分析与可视化
- 基于规则的异常检测与预警
- 智能巡检任务的自动生成与执行

**DataInsight 不做什么**：
- 不替代 BI 工具（如 Tableau、Power BI）的高级自助分析
- 不构建大数据中台或数据湖
- 不替代企业业务系统（ERP、CRM、MES）
- 不提供原始业务数据的增删改（仅读取与分析）

### 1.3 与通用 BI / 大数据平台的区别

| 维度 | 通用 BI 平台 | 大数据中台 | DataInsight |
|------|-------------|-----------|-------------|
| 核心能力 | 自助分析、报表 | 数据治理、资产目录 | 数据 → 分析 → 预警 → 处理闭环 |
| 用户 | 数据分析师 | 数据工程师 | 运营经理、设备管理员 |
| 数据量级 | 海量 | 海量 | 万～百万级 |
| 核心场景 | 制作报表 | 治理数据资产 | 发现异常 + 推动处理 |

---

## 2. 产品全景图

### 2.1 产品价值流

```
┌──────────────────────────────────────────────────────────────────────┐
│                        DataInsight 产品价值流                          │
│                                                                      │
│  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌────────┐ │
│  │ 企业数据  │──→│ 数据接入  │──→│ 数据治理  │──→│ 分析洞察  │──→│ 决策行动 │ │
│  │ (分散)   │   │ (统一)   │   │ (可信)   │   │ (可视)   │   │ (闭环)  │ │
│  └─────────┘   └─────────┘   └─────────┘   └─────────┘   └────────┘ │
│                                                                      │
│  解决什么问题？                                                        │
│  数据分散 → 数据缺失 → 分析低效 → 异常滞后 → 无法形成管理闭环              │
└──────────────────────────────────────────────────────────────────────┘
```

### 2.2 产品分层架构

```
┌─────────────────────────────────────────────────────────────────┐
│  Layer 4: 展示层 (Presentation)                                   │
│  运营工作台 │ 数据管理 │ 数据分析 │ 可视化看板 │ 预警中心 │ 智能巡检   │
├─────────────────────────────────────────────────────────────────┤
│  Layer 3: 业务服务层 (Business Services)                          │
│  指标引擎 │ 规则引擎 │ 预警引擎 │ 巡检引擎 │ 数据分析引擎            │
├─────────────────────────────────────────────────────────────────┤
│  Layer 2: 数据服务层 (Data Services)                              │
│  数据采集 │ 数据清洗 │ 数据标准化 │ 指标计算 │ 异常检测 │ 数据质量    │
├─────────────────────────────────────────────────────────────────┤
│  Layer 1: 基础设施层 (Infrastructure)                             │
│  MySQL (业务数据) │ Redis (缓存/会话) │ Docker Compose (部署)       │
│  认证授权 │ 日志审计 │ 任务调度 (APScheduler)                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 业务架构

### 3.1 核心业务域

```
┌──────────────────────────────────────────────────────┐
│                  DataInsight 业务域                    │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐           │
│  │ 用户与权限域│  │ 数据管理域 │  │ 分析看板域 │           │
│  │          │  │          │  │          │           │
│  │ · 用户管理 │  │ · 数据源  │  │ · 指标分析 │           │
│  │ · 角色管理 │  │ · 数据集  │  │ · 趋势分析 │           │
│  │ · 权限管理 │  │ · 数据质量 │  │ · 异常分析 │           │
│  │ · 操作日志 │  │ · 数据采集 │  │ · 可视化   │           │
│  └──────────┘  └──────────┘  └──────────┘           │
│                                                      │
│  ┌──────────┐  ┌──────────┐                          │
│  │ 预警域    │  │ 巡检域    │                          │
│  │          │  │          │                          │
│  │ · 预警规则│  │ · 巡检任务│                          │
│  │ · 实时预警│  │ · 异常设备│                          │
│  │ · 预警处理│  │ · 巡检记录│                          │
│  │ · 通知记录│  │ · 智能推荐│                          │
│  └──────────┘  └──────────┘                          │
└──────────────────────────────────────────────────────┘
```

### 3.2 业务域依赖关系

```
用户与权限域 ←── 所有域的基础（认证、授权、审计）
     │
     ▼
数据管理域 ←── 数据源头（所有分析和展示的数据基础）
     │
     ├────→ 分析看板域 ←── 数据消费（基于数据集进行分析和展示）
     │         │
     │         ├────→ 预警域 ←── 基于分析结果触发
     │         │         │
     │         │         └────→ 巡检域 ←── 基于预警自动创建巡检任务
     │         │
     │         └────→ 报表（基础导出能力，基于分析结果导出）
```

---

## 4. 应用架构

### 4.1 架构总览：模块化单体

MVP 阶段采用 **模块化单体架构（Modular Monolith）**，不拆分为微服务。前后端分离部署，后端按业务模块在代码层面拆分，共享同一个数据库连接和进程。

```
┌──────────────────────┐     HTTP/REST      ┌──────────────────────┐
│   Frontend (React)   │ ◄──────────────► │   Backend (FastAPI)   │
│   Port: 5173         │                    │   Port: 8000          │
└──────────────────────┘                    └──────────┬───────────┘
                                                       │
                                            ┌──────────┴───────────┐
                                            │          │           │
                                            ▼          ▼           ▼
                                      ┌─────────┐ ┌───────┐ ┌─────────┐
                                      │  MySQL   │ │ Redis  │ │ 本地存储  │
                                      │ Port:3306│ │Port:6379│ │ /exports │
                                      └─────────┘ └───────┘ └─────────┘
```

### 4.2 前端应用架构

```
┌─────────────────────────────────────────────────────┐
│                  Frontend SPA (React 18)             │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │              UI Layer (Ant Design 5)          │   │
│  │  Layout │ Navigation │ Table │ Form │ Modal   │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           Component Layer                     │   │
│  │  StatCard │ TrendChart │ DataTable            │   │
│  │  FilterBar │ AlertBadge │ WidgetGrid          │   │
│  │  PermissionGuard │ SearchBar │ ExportButton   │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           State Management                    │   │
│  │  Zustand (Global State)                       │   │
│  │  React Query / TanStack Query (Server State)  │   │
│  │  React Router v6 (URL State)                  │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           Service Layer                       │   │
│  │  API Client (Axios) │ Auth Service            │   │
│  │  Polling Service (轮询替代 WebSocket)         │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

> **MVP 说明**：实时更新采用前端轮询（Polling）方式，不引入 WebSocket。后续版本可升级为 WebSocket 推送。

### 4.3 后端应用架构

```
┌─────────────────────────────────────────────────────┐
│              Backend Server (Python / FastAPI)        │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           Router Layer (FastAPI APIRouter)    │   │
│  │  auth_router │ dashboard_router               │   │
│  │  datasource_router │ dataset_router           │   │
│  │  analysis_router │ alert_router               │   │
│  │  inspection_router │ user_router              │   │
│  │  role_router │ log_router                     │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           Middleware Layer                    │   │
│  │  Auth (JWT) │ RBAC │ RateLimit │ CORS         │   │
│  │  RequestLogger │ ExceptionHandler             │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           Service Layer                       │   │
│  │  AuthService │ DashboardService               │   │
│  │  DataSourceService │ DatasetService           │   │
│  │  AnalysisService │ MetricService              │   │
│  │  AlertService │ AlertRuleEngine               │   │
│  │  InspectionService │ ReportService            │   │
│  │  UserService │ PermissionService              │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           Data Access Layer                   │   │
│  │  SQLAlchemy ORM │ Redis Client (aioredis)     │   │
│  │  Pandas (数据分析计算)                         │   │
│  └─────────────────────────────────────────────┘   │
│                                                     │
│  ┌─────────────────────────────────────────────┐   │
│  │           Scheduled Tasks (APScheduler)       │   │
│  │  DataSyncTask │ MetricCalcTask                │   │
│  │  AlertCheckTask │ InspectionGenerateTask      │   │
│  └─────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────┘
```

### 4.4 模块通信方式（MVP）

| 通信场景 | 方式 | 说明 |
|---------|------|------|
| 前端 ↔ 后端 | RESTful API (HTTP) | 所有业务操作 |
| 实时数据更新 | 前端轮询 (Polling, 30s/60s) | Dashboard、预警列表刷新 |
| 后端 ↔ 外部数据源 | MySQL Driver / CSV Parser | 数据采集 |
| 异步任务 | APScheduler 进程内调度 | 数据同步、预警检测 |
| 服务间通信 | 进程内函数调用 | 模块化单体，不跨进程 |

---

## 5. 数据架构

### 5.1 数据流转全景

```
┌────────────────────────────────────────────────────────────────┐
│                        数据流转全景 (MVP)                        │
│                                                                │
│  [外部数据源]                                                    │
│   MySQL 数据库 │ CSV 文件上传                                    │
│       │                                                        │
│       ▼                                                        │
│  ┌─────────┐   ┌─────────┐   ┌──────────┐   ┌──────────┐      │
│  │ 数据采集  │──→│ 数据清洗  │──→│ 数据标准化 │──→│ 数据集存储 │      │
│  │ (读取)   │   │(去重/补全)│   │(格式/单位) │   │ (MySQL)  │      │
│  └─────────┘   └─────────┘   └──────────┘   └──────────┘      │
│                                                    │           │
│                                                    ▼           │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐   │
│  │ 数据质量  │←──│ 指标计算  │←──│ 数据分析  │←──│ 数据查询  │   │
│  │ (监控)   │   │(Pandas)  │   │(聚合/对比)│   │          │   │
│  └──────────┘   └──────────┘   └──────────┘   └──────────┘   │
│       │              │               │                         │
│       ▼              ▼               ▼                         │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐                   │
│  │ 质量报告  │   │ 指标快照  │   │ 分析结果  │                   │
│  └──────────┘   └──────────┘   └──────────┘                   │
│                                      │                         │
│                                      ▼                         │
│  ┌──────────┐   ┌──────────┐   ┌──────────┐                   │
│  │ 看板展示  │←──│ 预警触发  │←──│ 异常检测  │                   │
│  └──────────┘   └──────────┘   └──────────┘                   │
│       │              │                                         │
│       ▼              ▼                                         │
│  ┌──────────┐   ┌──────────────────┐                            │
│  │ CSV 导出  │   │ 智能巡检任务生成  │                            │
│  └──────────┘   └──────────────────┘                            │
└────────────────────────────────────────────────────────────────┘
```

### 5.2 数据分层（MySQL 逻辑分层）

MVP 阶段不搭建独立数据仓库，使用 MySQL 中不同表/逻辑层模拟数据分层：

| 层级 | 名称 | 说明 | MySQL 实现方式 |
|------|------|------|---------------|
| **ODS** | 原始数据层 | 从数据源采集的原始数据，保留源格式 | `raw_*` 前缀表，存储拉取的原数据 |
| **DWD** | 明细数据层 | 清洗、去重、标准化后的明细数据 | `datasets` + `dataset_fields` 定义的数据集表 |
| **DWS** | 汇总数据层 | 按维度和时间粒度聚合的指标数据 | `metric_values` 表，存储聚合后的指标快照 |
| **ADS** | 应用数据层 | 面向看板、预警、巡检的结果数据 | 业务表直接查询 + Redis 缓存热点数据 |

> **核心理念**：原始数据 → 清洗 → 标准化 → 指标数据 → 分析结果。分层通过表结构和业务逻辑实现，不引入额外的数仓组件。

---

## 6. 技术架构

### 6.1 技术选型总览

```
┌─────────────────────────────────────────────────────┐
│                    前端技术栈 (MVP)                    │
│  React 18 │ TypeScript 5 │ Vite 5                    │
│  Ant Design 5 │ ECharts 5 │ Axios                    │
│  Zustand │ React Query │ React Router v6             │
├─────────────────────────────────────────────────────┤
│                    后端技术栈 (MVP)                    │
│  Python 3.12 │ FastAPI │ Pandas                      │
│  SQLAlchemy (ORM) │ PyMySQL (Driver)                 │
│  APScheduler (定时任务) │ PyJWT (认证)                │
│  bcrypt (密码加密) │ Pydantic (数据校验)              │
├─────────────────────────────────────────────────────┤
│                    基础设施 (MVP)                      │
│  MySQL 8.0 │ Redis 7 │ Docker + Docker Compose       │
│  Nginx (反向代理) │ GitHub Actions (CI/CD)            │
└─────────────────────────────────────────────────────┘
```

### 6.2 关键技术决策

| 决策点 | 选择 | 理由 |
|--------|------|------|
| **前端框架** | React 18 + TypeScript | 成熟的生态系统，Ant Design 深度支持 |
| **构建工具** | Vite 5 | 快速 HMR，ESBuild 构建，取代 Webpack |
| **UI 库** | Ant Design 5 | 企业级中后台最成熟的 React 组件库 |
| **图表** | ECharts 5 | 国内企业数据可视化事实标准 |
| **状态管理** | Zustand + React Query | 轻量全局状态 + 专注服务端缓存 |
| **后端框架** | Python FastAPI | 高性能异步框架，自动生成 API 文档，类型安全 |
| **数据分析** | Pandas | Python 数据分析事实标准，DataFrame 操作便捷 |
| **ORM** | SQLAlchemy 2.0 | Python 最成熟的 ORM，支持异步 |
| **数据库** | MySQL 8.0 | 企业最广泛使用的关系型数据库，招聘友好 |
| **缓存** | Redis 7 | 会话管理 + 热点数据缓存 |
| **定时任务** | APScheduler | Python 轻量级进程内任务调度，无需额外服务 |
| **部署** | Docker Compose | 4 个容器一键启动（Nginx + Frontend + Backend + MySQL + Redis） |
| **认证** | JWT + bcrypt | 无状态认证，密码安全存储 |

### 6.3 前端路由设计（MVP 14 个核心页面）

```
/login                          → LoginPage
/dashboard                      → DashboardPage          （运营工作台）
/data-sources                   → DataSourceListPage     （数据源列表）
/datasets                       → DatasetListPage        （数据集列表）
/data-quality                   → DataQualityPage        （数据质量）
/data-analysis                  → DataAnalysisPage       （指标分析工作台，含趋势/对比/异常分析）
/dashboards                     → DashboardListPage      （看板列表）
/dashboards/:id                 → DashboardDetailPage    （看板详情）
/alerts                         → AlertListPage          （实时预警）
/alert-rules                    → AlertRulePage          （预警规则）
/inspections                    → InspectionTaskPage     （巡检任务）
/inspection-records             → InspectionRecordPage   （巡检记录）
/users                          → UserManagementPage     （用户管理）
/roles                          → RoleManagementPage     （角色权限）
/logs                           → LogPage                （操作日志）
```

> **页面收敛说明**：
> - `/trend-analysis`、`/compare-analysis`、`/anomaly-analysis` 合并入 `/data-analysis`（分析工作台统一入口，通过 Tab/Step 切换分析方式）
> - `/data-sources/:id` 详情通过抽屉/弹窗实现，不独立路由
> - `/datasets/:id` 同上
> - `/inspection-plans` 合并入 `/inspections`（计划在巡检任务页面内管理）
> - `/abnormal-devices` 合并入 `/inspections`（异常设备作为巡检列表的筛选状态）
> - `/reports` 取消独立模块，导出能力集成在各个页面的导出按钮
> - `/settings`、`/versions` 取消独立页面，系统配置通过配置文件管理
> - `/alerts/processed` 合并入 `/alerts`（通过 Tab 切换活跃/已处理）

### 6.4 后端 API 模块划分（MVP）

```
/api/auth                       认证模块
  POST   /login                 登录
  POST   /logout                登出
  GET    /me                    当前用户信息

/api/dashboard                  工作台模块
  GET    /overview              运营总览数据

/api/data-sources               数据源模块
  GET    /                      数据源列表
  POST   /                      新增数据源
  GET    /:id                   数据源详情
  PUT    /:id                   编辑数据源
  DELETE /:id                   删除数据源
  POST   /:id/test              测试连接
  POST   /:id/sync              手动同步

/api/datasets                   数据集模块
  GET    /                      数据集列表
  POST   /                      新增数据集
  GET    /:id                   数据集详情
  PUT    /:id                   编辑数据集
  DELETE /:id                   删除数据集
  GET    /:id/preview           数据预览

/api/data-quality               数据质量模块
  GET    /overview              质量总览
  GET    /:datasetId            数据集质量详情

/api/analysis                   分析模块（统一入口）
  POST   /execute               执行分析（参数指定分析类型：trend/compare/anomaly/ranking）

/api/dashboards                 看板模块
  GET    /                      看板列表
  POST   /                      创建看板
  GET    /:id                   看板详情（含组件数据）
  PUT    /:id                   编辑看板
  DELETE /:id                   删除看板

/api/alerts                     预警模块
  GET    /                      预警列表（支持 status=pending/processed 筛选）
  GET    /:id                   预警详情
  PUT    /:id/process           标记处理中
  PUT    /:id/close             关闭预警

/api/alert-rules                预警规则模块
  GET    /                      规则列表
  POST   /                      创建规则
  GET    /:id                   规则详情
  PUT    /:id                   编辑规则
  DELETE /:id                   删除规则
  PUT    /:id/toggle            启用/禁用

/api/inspections                巡检模块（计划+任务+记录统一管理）
  GET    /tasks                 巡检任务列表（含异常设备筛选）
  POST   /tasks                 手动创建巡检任务
  PUT    /tasks/:id/execute     执行巡检并记录结果
  GET    /records               巡检记录列表

/api/users                      用户管理
  GET    /                      用户列表
  POST   /                      新增用户
  GET    /:id                   用户详情
  PUT    /:id                   编辑用户
  PUT    /:id/status            禁用/启用

/api/roles                      角色管理
  GET    /                      角色列表
  POST   /                      新增角色
  GET    /:id                   角色详情
  PUT    /:id                   编辑角色
  PUT    /:id/permissions       配置权限

/api/logs                       日志模块
  GET    /                      操作日志列表（支持筛选）
```

> **API 收敛说明**：
> - `/api/analysis` 统一为一个 `POST /execute` 接口，通过请求参数中的 `analysisType` 区分趋势/对比/异常/排名
> - 取消独立的 `/api/reports` 模块，导出能力通过各模块的 `?export=csv` 参数实现
> - 取消独立的 `/api/inspections/plans`，巡检计划作为任务的一个配置属性
> - 取消 `/api/settings`、`/api/versions`，通过配置文件管理
> - 取消 `/api/auth/refresh`，改用长期 Token + 前端自动续期

---

## 7. 部署架构

### 7.1 MVP 部署方案（Docker Compose，5 个容器）

```
┌─────────────────────────────────────────────────────────┐
│                    Nginx (Reverse Proxy)                 │
│                    Port: 80                              │
└─────────┬───────────────────────────────┬───────────────┘
          │                               │
          ▼                               ▼
┌─────────────────┐             ┌─────────────────┐
│  Frontend        │             │  Backend API     │
│  (Nginx Static)  │────────────→│  (FastAPI)       │
│  Port: 5173      │   /api/*    │  Port: 8000      │
└─────────────────┘             └───────┬─────────┘
                                        │
                    ┌───────────────────┼───────────────────┐
                    │                   │
                    ▼                   ▼
          ┌─────────────┐   ┌─────────────────┐
          │    MySQL     │   │     Redis        │
          │  Port: 3306  │   │   Port: 6379     │
          └─────────────┘   └─────────────────┘
```

### 7.2 Docker Compose 服务清单

```yaml
services:
  nginx:        # 反向代理 + 前端静态文件
  backend:      # FastAPI 后端
  mysql:        # MySQL 8.0 数据库
  redis:        # Redis 7 缓存
```

> 不包含 Elasticsearch、MinIO、RabbitMQ、Prometheus、Grafana。导出文件存储在容器本地挂载卷 `/exports`。

---

## 8. 安全架构

### 8.1 安全层次

```
┌──────────────────────────────────────┐
│ Layer 1: 网络层                       │
│ · HTTPS 加密传输（生产环境）            │
│ · CORS 白名单                         │
│ · Rate Limiting（FastAPI 中间件）      │
├──────────────────────────────────────┤
│ Layer 2: 应用层                       │
│ · JWT Token 认证                      │
│ · RBAC 权限控制                       │
│ · 输入验证（Pydantic Schema）          │
│ · SQL 注入防护（SQLAlchemy 参数化）     │
│ · XSS 防护（React 默认转义）           │
│ · 操作日志审计                        │
├──────────────────────────────────────┤
│ Layer 3: 数据层                       │
│ · 密码 bcrypt 加密存储                 │
│ · 数据权限隔离（行级过滤）             │
│ · 软删除设计（deleted_at）            │
│ · 敏感配置环境变量管理                 │
└──────────────────────────────────────┘
```

### 8.2 权限模型（RBAC）

```
用户 (User)
  │
  ├── 角色 (Role) ── 权限 (Permission)
  │
用户 ←→ 角色 (多对多)
角色 ←→ 权限 (多对多)
```

**权限粒度**：
- 模块级：工作台 / 数据管理 / 数据分析 / ...
- 页面级：数据源列表 / 数据源详情 / ...
- 操作级：查看 / 新增 / 编辑 / 删除

---

## 9. 指标体系

### 9.1 MVP 产品业务指标

| 指标类别 | 指标名称 | 计算方式 | 目标值 |
|---------|---------|---------|--------|
| **数据质量** | 数据采集成功率 | 采集成功次数 / 采集总次数 × 100% | ≥ 99.5% |
| | 数据完整率 | 非空字段数 / 总字段数 × 100% | ≥ 98% |
| | 数据准确率 | 核验正确记录数 / 抽样总数 × 100% | ≥ 99% |
| | 数据重复率 | 重复记录数 / 总记录数 × 100% | < 1% |
| **系统性能** | Dashboard 首屏加载时间 | Lighthouse Performance Score | < 2s |
| | 千条数据分析耗时 | API 请求→响应完整时间 | < 5s |
| | 全量数据分析耗时 | 10 万条数据全表分析 | < 30s |
| | API P95 响应时间 | 95 分位响应时间 | < 500ms |
| **业务效果** | 异常发现率 | 系统发现异常数 / 已知异常总数 × 100% | ≥ 95% |
| | 预警触达率 | 成功触达预警数 / 预警总数 × 100% | ≥ 99% |
| | 预警处理率 | 已处理预警数 / 预警总数 × 100% | ≥ 90% |
| | 巡检完成率 | 已完成巡检数 / 计划巡检数 × 100% | ≥ 95% |
| | 预警平均处理时长 | Σ(关闭时间 - 触发时间) / 预警总数 | < 30min（严重级） |
| **报表** | 报表生成成功率 | 成功生成数 / 请求生成数 × 100% | ≥ 99% |
| | 报表平均生成时间 | Σ 生成耗时 / 报表总数 | < 10s |

> **MVP 聚焦**：指标集中在数据质量、系统性能、业务效果三类，不包含 DAU、NPS、用户留存等运营类指标（这些是产品运营阶段的指标，非 MVP 产品性能指标）。

### 9.2 Dashboard 指标业务逻辑

Dashboard 中的 KPI 指标之间存在严格的业务逻辑关系：

```
数据总量 (totalDataVolume)
  └── 所有数据源已采集数据行数之和

今日新增 (todayNewData)
  └── 所有数据源今日采集数据量之和

正常运行率 (normalOperationRate)
  = 状态为"正常"的设备数 / 总设备数 × 100%
  └── 设备状态来源于 device_status 字段

异常数量 (anomalyCount)
  = COUNT(alerts WHERE status IN ('pending', 'processing'))
  + COUNT(devices WHERE status IN ('abnormal', 'offline'))
  + COUNT(data_sources WHERE status = 'error')
  └── 派生指标，与预警中心、设备状态、数据源状态联动

设备利用率 (deviceUtilization)
  = SUM(实际运行时长) / SUM(计划运行时长) × 100%
  └── 基于设备运行数据计算

待处理预警数
  = COUNT(alerts WHERE status = 'pending')
  └── 与预警中心数据实时联动

待巡检设备数
  = COUNT(inspection_tasks WHERE status = 'pending')
  └── 与智能巡检数据联动

数据源异常数
  = COUNT(data_sources WHERE status = 'error' OR status = 'disconnected')
  └── 与数据源状态联动
```

---

## 10. 未来扩展能力

以下能力在 MVP 阶段不实现，但在架构设计上预留扩展空间。当产品验证通过、用户量和数据量增长后，可按优先级逐步引入。

### 10.1 扩展路线图

| 优先级 | 能力 | 当前 MVP 替代方案 | 引入时机 | 说明 |
|--------|------|------------------|---------|------|
| **P1** | WebSocket 实时推送 | 前端轮询（30s/60s） | v1.1 | 预警实时性要求提高时引入 |
| **P1** | MinIO / OSS 对象存储 | 本地文件系统 `/exports` | v1.2 | 导出文件量增大时引入 |
| **P2** | Elasticsearch 日志搜索 | MySQL LIKE 查询 | v1.2 | 操作日志量 > 100 万条时引入 |
| **P2** | RabbitMQ 消息队列 | APScheduler 进程内调度 | v2.0 | 数据同步任务量增大需要异步解耦时引入 |
| **P2** | PostgreSQL 数据库 | MySQL 8.0 | v2.0 | 需要高级分析函数（窗口函数、CTE）时评估 |
| **P3** | Prometheus + Grafana 监控 | Docker 日志 + 手动排查 | v2.0 | 系统需要 SLA 保障时引入 |
| **P3** | Kubernetes 编排 | Docker Compose | v2.0 | 需要多副本、自动扩缩容时引入 |
| **P3** | 微服务拆分 | 模块化单体 | v2.0+ | 团队规模 > 10 人、模块独立迭代需求明确时引入 |
| **P3** | 移动端 H5 适配 | 桌面端（1366×768+） | v2.0 | 移动巡检场景需求明确时引入 |

### 10.2 架构预留设计

当前 MVP 架构已经为未来扩展做了以下预留：

- **数据库**：使用 SQLAlchemy ORM，切换 PostgreSQL 只需修改连接串和少量方言差异
- **缓存**：Redis 已独立部署，未来可直接扩展为消息队列（Redis Streams）
- **后端模块化**：FastAPI APIRouter 天然支持按模块拆分，未来可独立部署为微服务
- **前端状态**：Zustand + React Query 分层清晰，未来 WebSocket 推送可直接替换轮询层
- **部署**：Docker Compose → K8s 迁移路径清晰，每个服务已容器化

---

> **文档维护**：本架构文档由技术团队维护，随产品迭代更新。
